{
  "name": "mini-ts-logger",
  "version": "1.0.3",
  "description": "A new minimal logger lib written in typescript because reasons",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "lock": "npm i --package-lock-only",
    "release": "npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "/lib"
  ],
  "author": "Guga Figueiredo",
  "devDependencies": {
    "@types/node": "^18.11.0"
  }
}
